"""web-archive-mcp — persistent web fetch and search archiving.

Saves every web_fetch and web_search result as timestamped JSONL files,
indexable by fst-indexer and searchable via unified-history-mcp.
"""
